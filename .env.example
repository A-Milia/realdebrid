# Optional: The Movie Database API key for richer metadata.
# Carátulas y sinopsis funcionan sin clave vía Cinemeta (público).
TMDB_API_KEY=

# Admin access (server-only — never commit real values)
# Your Real-Debrid API token from https://real-debrid.com/apitoken
RD_ADMIN_TOKEN=
# Password only you know to unlock admin login
ADMIN_PASSWORD=
